#Attention Is All You Need-Notes 

#What is the problem ? 
Old Models like CNN,RNN they are slow, process sequentially, struggle with long sentences , Hard to train efficiently. 

#What is the solution ? 
The Transformers - Uses Attention Meachanism, removes RNN, CNN , Process all words at once.

#Why it is better ?
fater training, Better Performance, Handle Long Range Dependencies,More efficient. 

